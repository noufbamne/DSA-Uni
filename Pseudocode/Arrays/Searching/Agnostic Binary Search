Algorithm FindSorted(arr, target)
Pre: arr is sorted in ascending OR descending order
Post: return index if found, else return -1

1. start = 0
2. end = length(arr)-1
3. isAsc = arr[start] < arr[end]
4. while start <= end do
       mid = start + (end - start) / 2
       if arr[mid] = target then
            return mid
       if isAsc then
            if target < arr[mid] then
                 end = mid - 1
            else
                 start = mid + 1
       else
            if target > arr[mid] then
                 end = mid - 1
            else
                 start = mid + 1
5. return -1
End Algorithm


(II)
Algorithm FindSorted(arr, target)
Pre: arr is sorted (ascending OR descending)
Post: returns index of target if found, else -1

start = 0
end = length(arr) - 1
isAsc = arr[start] < arr[end]   // true if ascending

While start <= end do
    mid = start + (end - start) / 2

    If arr[mid] = target then
        Return mid
    End If

    If isAsc then
        If target < arr[mid] then
            end = mid - 1
        Else
            start = mid + 1
        End If
    Else   // descending
        If target > arr[mid] then
            end = mid - 1
        Else
            start = mid + 1
        End If
    End If
End While

Return -1
End Algorithm


(III)
Algorithm FindSorted(arr, target)
Pre: arr is sorted in ascending or descending order
Post: return index if found, else return -1

Step 1: Set start = 0
Step 2: Set end = length(arr) - 1
Step 3: Check if array is ascending: isAsc = arr[start] < arr[end]
Step 4: While start <= end do
         Step 4.1: mid = start + (end - start)/2
         Step 4.2: If arr[mid] = target, return mid
         Step 4.3: If isAsc = true then
                     If target < arr[mid], set end = mid - 1
                     Else, set start = mid + 1
                 Else (descending)
                     If target > arr[mid], set end = mid - 1
                     Else, set start = mid + 1
Step 5: If not found, return -1
End Algorithm
